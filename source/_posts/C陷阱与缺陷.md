---
title: C陷阱与缺陷笔记
date: 2025-11-10 16:14:03
tags:
 - C/C++
catagories:
 - tutorial
---
## 1.词法“陷阱”

#### 1.0 两个定义
**符号**（token）：程序的基本组成单元，相同的符号在不同的上下文中解释不同
**空白字符**（Whitespace Character）：常见的空白字符有空格符、制表符和换行符，它们都可以作为*分割不同符号的空白*使用
#### 1.1 =不同于==
注意：当在if语句中使用赋值符号并判断其结果是，应该显式使用比较符号，说明这个赋值结果需要比较。
- 如if (x = y)应写成 if ((x = y) != 0)

#### 1.2 &与| 不同于 &&与||
&与|为按位运算符，用于运算
&&与||用于比较

#### 1.3 词法分析中的“贪心法”
既然程序由一个个符号组成，那么C编译器该如何区分每个符号呢。
- 当符号被清晰地用空白字符分割时，从一个分割点到下一个分割点为一个符号
- 当符号连在一起产生词义混淆时，如a---，C编译器此时会使用贪心法读取符号：**每个符号应该包含尽可能多的字符**，则会翻译成a-- -

#### 1.4 整型常量
如果一个整形常量以0开始，那么编译器会把它当成八进制

#### 1.x 练习
---
（1）主流语言注释语法都是**不可嵌套的**，但是呢注释有些时候不止用来注释，还被人用来屏蔽代码块，这是不好的行为，因为可能导致注释嵌套
正确的方法是使用宏屏蔽直接忽略代码块
```
#if 0
	xxx
	xxx
#endif
```
---
（2）我会，因为直接把代码块注释掉更方便。

---
（3）因为编译器读取符号时的贪心法则

---
（4）根据贪心法则，被解释为(a++)++ + b，但++必须有左值或者右值，所以该代码无法运行

## 2.语法“陷阱”
#### 2.0 说明
**语法**：符号组成的句子的含义

#### 2.1 理解函数声明
**C声明的两部分**：类型+类似表达符的声明符

下面我们来解释语句：`(*(void(*)()0))();`
- 第一部分：转换符
	- 如何得到一个对应类型的转换符？只需要在这种类型声明定义的基础上，去掉函数名即可
	- 如：`void (*ptr)(void)`的意思是，一个指向返回值和接受值都为void的函数指针，将名字去掉，并且扩起括号，我们就得到了这种类型的转换符。`(void (*)(void))`
- 第二部分：函数指针
	- 如何运行一个函数指针？`(*ptr)()`
- 将两个结合起来，就得到了该表达式

#### 2.3 作为语句结束标志的分号
`;`，单独的分号会被视为一个空语句

**错误情况1**
循环或者条件语句后多引号
```
if (x > 0);
    x = -1;
```
当while或者if语句后接了一个引号，只会执行空引号的内容

**错误情况2**
return后缺引号
```
if (n < 3)
    return
lo.date = x[0];
lo.date = x[1];
```
当n >= 3时，lo.date = x[0]永远不会执行，这种错误非常难发现

**错误情况3**
结构后缺引号
```
struct lo {
    int date;
    int time;
    int code;
}

main(){
    ...
}
```
结构后紧接了一个函数定义
那么结构lo会被当成是main函数的返回值

#### 2.4 switch语句
在C中，case会当成一个条件分支处理，因此程序流会按顺序执行case后的语句，需要跳出，就得使用break
这个特性，能够在程序处理多个问题，而问题的处理方式差别不大时，提供帮助，如
```
switch (ch){
    case '\n':
        line++;
        /*此处没有break语句*/
    case '\t':
        /*此处没有break语句*/
    case ' ':
        相同的处理...
        break;
}
```
该程序在遇到三种字符时都进行相同的处理，只不过换行符需要额外执行line++操作
同时，如果case后面没有break，则需要手动用注释写明

#### 2.5 悬挂else引起的问题
```
if (x == 0)
    if (y == 0) error();
else
{
    z = x + y;
    f(&z);
}
```
如果不加大括号明确表明结构的话，if会结合离它最近的else
因此，上面的程序实际上是
```
if (x == 0){
    if (y == 0)
        error();
    else
    {
        z = x + y;
        f(&z);
    }
}
```
所以尽量把括号都加起